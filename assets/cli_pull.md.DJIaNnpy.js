import{_ as a,o as s,c as t,a4 as n}from"./chunks/framework.DpieW3qv.js";const u=JSON.parse('{"title":"pull","description":"","frontmatter":{},"headers":[],"relativePath":"cli/pull.md","filePath":"cli/pull.md","lastUpdated":null}'),l={name:"cli/pull.md"};function i(r,e,d,o,h,p){return s(),t("div",null,[...e[0]||(e[0]=[n(`<h1 id="pull" tabindex="-1">pull <a class="header-anchor" href="#pull" aria-label="Permalink to &quot;pull&quot;">​</a></h1><p>Git sync + fetch data + auto-build new dates, then open the status hub. Your daily starting command.</p><h2 id="usage" tabindex="-1">Usage <a class="header-anchor" href="#usage" aria-label="Permalink to &quot;Usage&quot;">​</a></h2><div class="language-bash vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">make</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> pull</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">                    # Full: git + fetch + auto-build + open status hub</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">make</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> pull</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> SKIP_FETCH=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">1</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">       # Skip data fetch; git only, then open status</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">make</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> pull</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> CONVERT=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">1</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">          # (Smartsheet) Fetch + convert to docs/excel, then parse</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br></div></div><h2 id="what-it-does" tabindex="-1">What It Does <a class="header-anchor" href="#what-it-does" aria-label="Permalink to &quot;What It Does&quot;">​</a></h2><ol><li><strong><code>git pull --rebase</code></strong> — syncs with teammates&#39; committed work</li><li><strong>Fetch from data source</strong> — parses Excel/Smartsheet into refresh files (progress spinner: &quot;Fetching data … Parsing {file}&quot;)</li><li><strong>Auto-build</strong> — builds only dates that don&#39;t yet have an <code>ocapi-payload.json</code> (not every run). Progress shows &quot;Building new dates … 1/N … date/refreshId&quot; or &quot;All dates already built (only missing builds run).&quot;</li><li><strong>Open status hub</strong> — you land on the status dashboard where you pick a refresh and run an action (preview, push, etc.)</li></ol><p>There is no separate date picker in pull; the status hub is the single place to pick your working refresh after fetch and build.</p><h2 id="example-output" tabindex="-1">Example Output <a class="header-anchor" href="#example-output" aria-label="Permalink to &quot;Example Output&quot;">​</a></h2><div class="language- vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>  • git pull --rebase</span></span>
<span class="line"><span>  Already up to date.</span></span>
<span class="line"><span></span></span>
<span class="line"><span>  ⟳ Fetching data … Parsing LOFT (Converted).xlsx</span></span>
<span class="line"><span>  ✔ Data fetched.</span></span>
<span class="line"><span></span></span>
<span class="line"><span>  ⟳ Building new dates … 2/3 … 2026-02-14/r1</span></span>
<span class="line"><span>  ✔ Built 1 refresh.</span></span>
<span class="line"><span></span></span>
<span class="line"><span>  (status hub opens)</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br><span class="line-number">9</span><br><span class="line-number">10</span><br></div></div><p>When all dates are already built you&#39;ll see: <code>✔ All dates already built (only missing builds run).</code> then the status hub.</p><h2 id="fetch-and-build-behavior" tabindex="-1">Fetch and build behavior <a class="header-anchor" href="#fetch-and-build-behavior" aria-label="Permalink to &quot;Fetch and build behavior&quot;">​</a></h2><ul><li><strong>Fetch</strong> — Always runs (unless <code>SKIP_FETCH=1</code>). Parses Excel/Smartsheet and writes/updates <code>refresh.json</code> per date.</li><li><strong>Auto-build</strong> — Only builds refreshes that don&#39;t have <code>ocapi-payload.json</code> yet. Existing builds are left as-is unless you use &quot;Force rebuild&quot; from the status hub. So pull does not rebuild every date every time.</li></ul><table tabindex="0"><thead><tr><th>Date state</th><th>Behavior</th></tr></thead><tbody><tr><td><strong>SYNCED</strong></td><td><code>refresh.json</code> updated if data changed; if so, marked <strong>UPDATED</strong> (stale) — rebuild and <code>make push</code> proceed automatically without <code>FORCE=1</code></td></tr><tr><td><strong>Already built</strong></td><td><code>refresh.json</code> updated if data changed (may need rebuild)</td></tr><tr><td><strong>New</strong></td><td>Written fresh; auto-build runs for these</td></tr></tbody></table><h3 id="automatic-stale-detection" tabindex="-1">Automatic stale detection <a class="header-anchor" href="#automatic-stale-detection" aria-label="Permalink to &quot;Automatic stale detection&quot;">​</a></h3><p>When <code>make pull</code> writes new data over a refresh that was already synced to SFCC, the system automatically marks it as <strong>stale</strong> (the status dashboard shows this as <strong>UPDATED</strong>, the display label for the stale state). This means:</p><ul><li><strong>Rebuild</strong> proceeds without <code>FORCE=1</code> — you&#39;ll see: <code>🔄 New data detected since last sync — rebuilding.</code></li><li><strong><code>make push</code></strong> proceeds without <code>FORCE=1</code> — you&#39;ll see: <code>🔄 Re-syncing — source data changed since last push.</code></li><li>After a successful push, the stale flag is cleared and the refresh returns to <code>SYNCED</code>.</li></ul><p>If the pull brings <strong>identical data</strong> (no changes), the refresh stays <code>SYNCED</code> and the guards remain active — use <code>make status</code> → &quot;Force rebuild&quot; for manual overrides like template-only changes.</p><h2 id="options" tabindex="-1">Options <a class="header-anchor" href="#options" aria-label="Permalink to &quot;Options&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Flag</th><th>Description</th></tr></thead><tbody><tr><td><code>SKIP_FETCH=1</code></td><td>Skip the data fetch step — only git pull, then open status</td></tr><tr><td><code>CONVERT=1</code></td><td><strong>(Smartsheet only)</strong> Fetch sheet, run through convert-excel, save to <code>docs/excel/{Brand} (Converted) - Smartsheet.xlsx</code>, then parse. Same golden format as local examples.</td></tr><tr><td><code>BRAND=ann</code></td><td>Override brand</td></tr><tr><td><code>FILE=&quot;path.xlsx&quot;</code></td><td>Override Excel file</td></tr></tbody></table><h2 id="related" tabindex="-1">Related <a class="header-anchor" href="#related" aria-label="Permalink to &quot;Related&quot;">​</a></h2><ul><li><a href="/ocapi-docs.github.io/cli/status.html"><code>status</code></a> — Pick a refresh (sets active) and run an action</li><li><a href="/ocapi-docs.github.io/cli/build.html"><code>build</code></a> — Build the selected date</li></ul>`,21)])])}const b=a(l,[["render",i]]);export{u as __pageData,b as default};
