import{_ as t,o as s,c as a,a4 as i}from"./chunks/framework.DpieW3qv.js";const u=JSON.parse('{"title":"Developer Guide","description":"","frontmatter":{"title":"Developer Guide"},"headers":[],"relativePath":"guides/developer-guide.md","filePath":"guides/developer-guide.md","lastUpdated":null}'),o={name:"guides/developer-guide.md"};function n(d,e,r,c,l,h){return s(),a("div",null,[...e[0]||(e[0]=[i(`<h1 id="developer-guide" tabindex="-1">Developer Guide <a class="header-anchor" href="#developer-guide" aria-label="Permalink to &quot;Developer Guide&quot;">​</a></h1><p>How the CLI is built and how to add or change behavior. For architecture detail, see <a href="/ocapi-docs.github.io/reference/architecture.html">Architecture</a>.</p><hr><h2 id="architecture-overview" tabindex="-1">Architecture overview <a class="header-anchor" href="#architecture-overview" aria-label="Permalink to &quot;Architecture overview&quot;">​</a></h2><p>The app uses <strong>Clean Architecture</strong>-style layers:</p><table tabindex="0"><thead><tr><th>Layer</th><th>Location</th><th>Purpose</th></tr></thead><tbody><tr><td><strong>CLI</strong></td><td><code>src/cli/</code></td><td>Entry point, command routing, flags, output</td></tr><tr><td><strong>Application</strong></td><td><code>src/application/usecases/</code></td><td>Workflows (Excel, Dev, SFCC, Reporting, Health)</td></tr><tr><td><strong>Domain</strong></td><td><code>src/domain/</code></td><td>Entities (Campaign, ContentAsset, SlotConfiguration), validation</td></tr><tr><td><strong>Infrastructure</strong></td><td><code>src/infrastructure/</code></td><td>File system, OCAPI client, parsing, rendering, registry</td></tr></tbody></table><p>The CLI is thin: it parses arguments, builds a <code>BrandContext</code>, and delegates to use cases.</p><hr><h2 id="cli-entrypoint" tabindex="-1">CLI entrypoint <a class="header-anchor" href="#cli-entrypoint" aria-label="Permalink to &quot;CLI entrypoint&quot;">​</a></h2><ul><li><strong>File:</strong> <code>src/cli/index.js</code></li><li><strong>Responsibilities:</strong> Parse <code>process.argv</code>, resolve brand (from config or <code>BRAND=</code>), parse flags with <code>parseFlags()</code> and <code>buildOptions()</code>, create <code>BrandContext</code>, dispatch to the right handler in <code>src/cli/commands/</code>.</li><li><strong>Makefile as source of truth:</strong> The app is controlled only via make (e.g. <code>make build</code>, <code>make push</code>). The Makefile is the canonical interface; see <a href="/ocapi-docs.github.io/reference/commands.html#make-variables-cli-flags">Commands Reference</a> for the Make variable → CLI flag table.</li></ul><hr><h2 id="command-structure" tabindex="-1">Command structure <a class="header-anchor" href="#command-structure" aria-label="Permalink to &quot;Command structure&quot;">​</a></h2><ul><li><strong>Handlers:</strong> One file per command under <code>src/cli/commands/</code> (e.g. <code>handleRefresh.js</code>, <code>handleSync.js</code>).</li><li><strong>Pattern:</strong> Each handler receives <code>(brand, target, options, ...)</code>. It uses <code>resolveRefreshForCommand</code> or similar when it needs a single refresh, then calls a use case on <code>brand.usecases.*</code>.</li><li><strong>Adding a command:</strong> See <a href="#adding-a-new-command">Adding a new command</a> below.</li></ul><hr><h2 id="adding-a-new-command" tabindex="-1">Adding a new command <a class="header-anchor" href="#adding-a-new-command" aria-label="Permalink to &quot;Adding a new command&quot;">​</a></h2><ol><li><strong>Register in the router:</strong> In <code>src/cli/index.js</code>, add the command to <code>COMMAND_MAP</code> and add a <code>case</code> in <code>runCommand()</code> that calls your new handler.</li><li><strong>Create the handler:</strong> Add <code>src/cli/commands/handleMyCommand.js</code>. Resolve brand/target/options (use <code>resolveRefreshForCommand</code> if the command needs a refresh), then call the appropriate use case.</li><li><strong>Use case (if needed):</strong> If the command needs new business logic, add a use case in the right workflow file under <code>src/application/usecases/</code>, and expose it via <code>BrandContext</code>.</li><li><strong>Make target (optional):</strong> In the Makefile, add a target that runs <code>$(CLI) myCommand $(BRAND) ...</code> with the right flags.</li><li><strong>Document:</strong> Add <code>docs/cli/my-command.md</code> and link it from <a href="/ocapi-docs.github.io/cli/">Commands</a> and <a href="/ocapi-docs.github.io/reference/commands.html">Commands Reference</a>.</li></ol><hr><h2 id="brandcontext-and-dependency-injection" tabindex="-1">BrandContext and dependency injection <a class="header-anchor" href="#brandcontext-and-dependency-injection" aria-label="Permalink to &quot;BrandContext and dependency injection&quot;">​</a></h2><ul><li><strong>BrandContext</strong> (<code>src/application/context/BrandContext.js</code>) is the composition root: it holds the brand id, file store, SFCC config, and <strong>usecases</strong> (Excel, Dev, Sfcc, Reporting, Health).</li><li>Handlers receive a <code>BrandContext</code> instance; they don&#39;t construct use cases themselves. This keeps the CLI testable (you can pass a mock or test brand).</li><li>Config is loaded from <code>app.config</code>, <code>slots.config</code>, <code>sfcc.config</code>, and user config (<code>.ocapi-config.json</code>, <code>.env</code>).</li></ul><hr><h2 id="barrel-files-index-js-re-exports" tabindex="-1">Barrel files (index.js re-exports) <a class="header-anchor" href="#barrel-files-index-js-re-exports" aria-label="Permalink to &quot;Barrel files (index.js re-exports)&quot;">​</a></h2><p>Directories with multiple modules use barrel files (<code>index.js</code>) to simplify imports. Instead of importing from individual files, consumers import from the directory:</p><div class="language-javascript vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">javascript</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// Instead of:</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">const</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">ApiClient</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> } </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> require</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;./ApiClient/ApiClient&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">const</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">RateLimiter</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> } </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> require</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;./ApiClient/RateLimiter&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// Use the barrel:</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">const</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">ApiClient</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">RateLimiter</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> } </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> require</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;./ApiClient&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br></div></div><p>Barrel files exist in: <code>src/config/</code>, <code>src/domain/</code>, <code>src/infrastructure/sfcc/ApiClient/</code>, <code>src/infrastructure/sfcc/OcapiServices/</code>, <code>src/utils/helpers/</code>, and others.</p><blockquote><p><strong>Circular dependency caution:</strong> Some helpers (<code>action.js</code>, <code>ipid.js</code>, <code>excel.js</code>) import directly from their specific config file (e.g. <code>../../config/app.config</code>) instead of the config barrel to avoid circular dependency loops.</p></blockquote><hr><h2 id="ocapi-runtime-configuration" tabindex="-1">OCAPI runtime configuration <a class="header-anchor" href="#ocapi-runtime-configuration" aria-label="Permalink to &quot;OCAPI runtime configuration&quot;">​</a></h2><p>All OCAPI tuning constants (rate limits, retries, timeouts, cache TTL) are centralized in <code>OCAPI_RUNTIME</code> in <code>src/config/sfcc.config.js</code>. These are hardcoded constants -- not environment variables -- to ensure consistent behavior:</p><table tabindex="0"><thead><tr><th>Constant</th><th>Value</th><th>Purpose</th></tr></thead><tbody><tr><td><code>maxConcurrent</code></td><td>3</td><td>Max parallel OCAPI requests</td></tr><tr><td><code>minTimeMs</code></td><td>100</td><td>Min ms between requests</td></tr><tr><td><code>requestTimeoutMs</code></td><td>30,000</td><td>HTTP request timeout</td></tr><tr><td><code>authTimeoutMs</code></td><td>20,000</td><td>Auth request timeout</td></tr><tr><td><code>maxRetries</code></td><td>3</td><td>API retry attempts</td></tr><tr><td><code>retryDelayMs</code></td><td>1,000</td><td>Base retry delay (exponential backoff)</td></tr><tr><td><code>maxBackoffMs</code></td><td>10,000</td><td>Max exponential backoff</td></tr><tr><td><code>tokenMaxRetries</code></td><td>4</td><td>Token fetch retry attempts</td></tr><tr><td><code>tokenBaseDelayMs</code></td><td>500</td><td>Token retry base delay</td></tr><tr><td><code>tokenMaxDelayMs</code></td><td>10,000</td><td>Token retry max delay</td></tr><tr><td><code>tokenSafetyWindowMs</code></td><td>60,000</td><td>Refresh token before expiry</td></tr><tr><td><code>batchChunkSize</code></td><td>50</td><td>Chunk size for bulk individual requests</td></tr><tr><td><code>paginationCount</code></td><td>50</td><td>Items per page in search results</td></tr><tr><td><code>paginationMaxPages</code></td><td>100</td><td>Max pages to fetch</td></tr><tr><td><code>cacheTtlMs</code></td><td>300,000</td><td>Client-side cache TTL (5 min)</td></tr><tr><td><code>safeBatchLimit</code></td><td>5</td><td>Use-case batch concurrency</td></tr></tbody></table><hr><h2 id="token-persistence" tabindex="-1">Token persistence <a class="header-anchor" href="#token-persistence" aria-label="Permalink to &quot;Token persistence&quot;">​</a></h2><p>OAuth tokens are cached to <code>dev-env/.token-cache.json</code> by <code>AuthService</code> so that consecutive CLI invocations reuse the same token instead of authenticating each time. The token is refreshed 60 seconds before expiry. See <a href="/ocapi-docs.github.io/guides/env-and-auth.html#token-caching">Environment Setup</a> for details.</p><hr><h2 id="testing-strategy" tabindex="-1">Testing strategy <a class="header-anchor" href="#testing-strategy" aria-label="Permalink to &quot;Testing strategy&quot;">​</a></h2><ul><li><strong>Unit tests:</strong> Under <code>tests/</code> — config, domain, infrastructure, usecases, utils (strings, dates, ipid, path, excel helpers). Vitest; include pattern <code>tests/**/*.test.js</code>.</li><li><strong>CLI tests:</strong> Under <code>tests/cli/</code> — command routing, guards, prompts. Use mocks for file system and OCAPI where needed.</li><li><strong>Integration:</strong> <code>tests/integration/workflow.test.js</code> and similar for full flows. When <code>dev-env</code> is not populated (e.g. in CI), workflow tests use <code>tests/fixtures/workflow/refresh.json</code> so they always run.</li><li><strong>Workflow (end-user flow):</strong> <code>make test-brands</code> runs both LOFT and Ann from a clean dev-env (excel → build → preview). For a single-brand test that also exercises the build guard and force rebuild: <code>node scripts/test-workflow-from-clean.js [loft|ann]</code>.</li><li><strong>Test commands (Makefile is source of truth):</strong> See <a href="/ocapi-docs.github.io/reference/commands.html">Commands Reference</a>. Summary: <code>make test</code> (run all), <code>make test-watch</code> (watch mode), <code>make test-coverage</code> (coverage report), <code>make test-brands</code> (E2E both brands), <code>make check</code> (lint + tests + audit for CI), <code>make test-sync DATE=</code> (push to OCAPI with <code>--yes --force</code> for CI / non-interactive). To run a subset: <code>npx vitest run tests/domain</code> or <code>npx vitest run tests/cli</code>.</li></ul><hr><h2 id="code-conventions" tabindex="-1">Code conventions <a class="header-anchor" href="#code-conventions" aria-label="Permalink to &quot;Code conventions&quot;">​</a></h2><ul><li><strong>Lint:</strong> ESLint (see <code>.eslintrc.json</code>). Run <code>npm run lint</code> or <code>make check</code> (lint + tests + audit).</li><li><strong>Format:</strong> Prettier (see <code>.prettierrc</code>). Run <code>npm run format</code>.</li><li><strong>JSDoc:</strong> Used for public APIs and for generating <a href="/ocapi-docs.github.io/reference/api/">API Reference</a>. Run <code>make docs-build</code> to regenerate API docs.</li></ul><hr><h2 id="related" tabindex="-1">Related <a class="header-anchor" href="#related" aria-label="Permalink to &quot;Related&quot;">​</a></h2><ul><li><a href="/ocapi-docs.github.io/reference/architecture.html">Architecture</a> — Full system design and layer details</li><li><a href="/ocapi-docs.github.io/reference/api/">API Reference</a> — Generated from JSDoc</li><li><a href="/ocapi-docs.github.io/operations/runbook.html">Runbook</a> — Release checklist and operations</li></ul>`,41)])])}const g=t(o,[["render",n]]);export{u as __pageData,g as default};
